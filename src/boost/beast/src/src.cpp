//
// Boost.Beast is header-only by default. This translation unit provides the
// out-of-line definitions for the separate compilation mode, which the
// component enables via BOOST_BEAST_SEPARATE_COMPILATION when
// CONFIG_BOOST_BEAST_ENABLED is set.
//

#include <boost/beast/src.hpp>
